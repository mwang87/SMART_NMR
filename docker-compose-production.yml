version: '3'
services:
  web:
    networks:
      - default
      - nginx-net
    environment:
      VIRTUAL_HOST: smart-dev.ucsd.edu
      VIRTUAL_PORT: 6213
      LETSENCRYPT_HOST: smart-dev.ucsd.edu
      LETSENCRYPT_EMAIL: miw023@ucsd.edu

networks:
  nginx-net:
    external:
      name: nginx-net